# Leave unset to run the dashboard against bundled demo data (shaped exactly
# like the research-agent benchmark). Set both to point it at a real API.
NEXT_PUBLIC_COMPUTELAYER_API_URL=http://localhost:8000/v1
NEXT_PUBLIC_COMPUTELAYER_API_KEY=cl_test_local_development_key
NEXT_PUBLIC_COMPUTELAYER_PROJECT=research-agent
